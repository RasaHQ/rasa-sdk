Sanic 25 action-server startup no longer crashes with ``Can't pickle local object 'run.<locals>._app_factory'``. The AppLoader factory is now a module-level function, and the default single-worker path uses ``Sanic.serve_single``.
