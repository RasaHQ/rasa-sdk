The `rasa/rasa-sdk` Docker image is now built on Ubuntu 24.04 with Python 3.12, aligning the action-server image with Rasa Pro 3.18+. The supported Python range for the `rasa-sdk` package itself is unchanged (`>=3.10,<3.15`).
