Dropped Python 3.14 support temporarily so the supported range matches Rasa Pro for the upcoming release (`>=3.10,<3.14`). Restores Sanic `^22.12` / `sanic-testing ~23.6` and the pre-#1361 action-server startup path. Python 3.14 will be re-introduced once Rasa Pro support is ready.
