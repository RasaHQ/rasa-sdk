Fixed the Docker image build failing during pip installation. The `Dockerfile` verified `get-pip.py` against a pinned checksum, which stopped matching once the pip 26.2 release regenerated the file published at `https://bootstrap.pypa.io/get-pip.py`. The checksum is now up to date.
